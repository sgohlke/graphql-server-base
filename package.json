{
  "name": "@sgohlke/graphql-server-base",
  "version": "2.1.2",
  "description": "Base package for @dreamit/graphql-server",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "scripts": {
    "build": "tsc",
    "check": "tsc --noEmit --pretty",
    "lint": "eslint src/*.ts src/**/*.ts",
    "lintfix": "eslint --fix src/*.ts src/**/*.ts",
    "prepack": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sgohlke/graphql-server-base.git"
  },
  "keywords": [
    "GraphQL",
    "server"
  ],
  "author": "Stefan Gohlke",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/sgohlke/graphql-server-base/issues"
  },
  "homepage": "https://github.com/sgohlke/graphql-server-base#readme",
  "devDependencies": {
    "@sgohlke/ts-base-dependencies": "1.0.9"
  },
  "peerDependencies": {
    "graphql": "^16.0.0"
  }
}
